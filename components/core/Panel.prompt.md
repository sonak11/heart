Content card with an optional titled header (eyebrow + title + action slot). The default container for charts, tables, and grouped content.

```jsx
<Panel eyebrow="Theme intelligence" title="Top barriers by circle" action={<Button size="sm" variant="ghost">Export</Button>}>
  …chart…
</Panel>
```

Set `padded={false}` for edge-to-edge tables.

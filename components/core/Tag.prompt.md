Monospace code chip. Use for code IDs (BARR-04), interview IDs, and active filter tokens.

```jsx
<Tag>BARR-04</Tag>
<Tag removable onRemove={clear}>Circle 1</Tag>
```

Set `removable` + `onRemove` to render a dismiss control.
